services:
  backend:
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - local.env
    ports:
      - "8000:8000"

  channels:
    env_file:
      - local.env

  db:
    env_file:
      - local.env
    ports:
      - "5432:5432"